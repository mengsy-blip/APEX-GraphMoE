# LoRA Adapter Artifacts

This directory is reserved for the fine-tuned LoRA adapter.

The original local artifact directory was:

```text
E:/experiments/extract_llm_training_dataset/llm_finetuning/lora_model
```

The adapter directory is about 548 MB, and some checkpoint files exceed the normal GitHub file-size limit. Store it locally, in Git LFS, or in a model registry such as Hugging Face Hub when publishing the complete artifact.
